apiVersion: v1
kind: Service
metadata:
  name: velaclaw
  labels:
    app: velaclaw
spec:
  type: ClusterIP
  selector:
    app: velaclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
