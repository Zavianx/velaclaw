import { definePluginEntry } from "velaclaw/plugin-sdk/plugin-entry";
// TTS removed — speech-provider.js deleted

export default definePluginEntry({
  id: "microsoft",
  name: "Microsoft",
  description: "Bundled Microsoft plugin (speech provider removed)",
  register() {
    // TTS removed — no providers to register
  },
});
